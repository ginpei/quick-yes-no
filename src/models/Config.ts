export const config = {
  /**
   * Standard animation duration in ms.
   * Also available as `--duration` in `:root` in CSS.
   */
  duration: 300,

  /**
   * Candidate image width.
   */
  candidateImageWidth: 64,
};
